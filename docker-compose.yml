services:
  db:
    image: postgres:16
    container_name: louisiana_pg
    environment:
      POSTGRES_USER: louisiana
      POSTGRES_PASSWORD: louisiana
      POSTGRES_DB: louisiana_routes
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
